# Supabase
# Get these from https://app.supabase.com → your project → Settings → API
SUPABASE_URL=https://REPLACE_ME.supabase.co
SUPABASE_KEY=REPLACE_ME_WITH_ANON_PUBLIC_KEY

# Groq — OCR / invoice extraction (server-side only, NEVER expose to browser)
# Get a key at https://console.groq.com/keys (free tier, no card required)
GROQ_API_KEY=REPLACE_ME
